% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{paginate_consort}
\alias{paginate_consort}
\title{Split a long CONSORT flowchart across multiple pages}
\usage{
paginate_consort(
  tracker,
  page_height_mm = 257,
  na_cells = NULL,
  step_labels = NULL,
  group_labels = NULL,
  font_size = 1,
  box_width = NULL,
  excl_width = NULL
)
}
\arguments{
\item{tracker}{A \code{cohort} object or tracker tibble (same as \code{\link[=consort_plot]{consort_plot()}}).}

\item{page_height_mm}{Usable page height in mm. Default \code{257} (A4 with
standard top/bottom margins). For US Letter use approximately \code{241}.}

\item{na_cells, step_labels, group_labels, font_size, box_width, excl_width}{Passed through to \code{\link[=consort_plot]{consort_plot()}} for each page.}
}
\value{
A list of \code{consort_grob} objects. Each element also carries a
\code{.page_info} attribute: \verb{list(page = i, n_pages = n, steps = <chr>)}.
}
\description{
For diagrams that exceed a page height (e.g. many steps with many groups),
this function renders the tracker as a list of \link{consort_grob} objects, one
per page. Steps are split greedily: each page holds as many steps as fit
within \code{page_height_mm}. The last step of every page is repeated as the
first step of the next page (overlap), so the reader always sees where
each page continues from.
}
\details{
Save the result with \code{\link[=save_consort_pages]{save_consort_pages()}}.
}
\examples{
\dontrun{
pages <- paginate_consort(cohort, page_height_mm = 257)
save_consort_pages(pages, "output/consort")
}

}
