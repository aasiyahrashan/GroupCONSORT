% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_trackers.R
\name{merge_trackers}
\alias{merge_trackers}
\title{Merge tracker tibbles from independently-processed datasets}
\usage{
merge_trackers(..., step_order = NULL, group_order = NULL)
}
\arguments{
\item{...}{Two or more tracker tibbles from \code{\link[=get_tracker]{get_tracker()}}. Each must have
columns \code{group}, \code{step}, \code{n_remaining}, \code{n_dropped}.}

\item{step_order}{Optional character vector giving the desired display order
of steps. Defaults to the order steps are first encountered across all
trackers.}

\item{group_order}{Optional character vector giving the desired display order
of groups. Defaults to the order groups are first encountered.}
}
\value{
A named list with two elements:
\describe{
\item{\code{tracker}}{A single tracker tibble with columns \code{group}, \code{step},
\code{n_remaining}, \code{n_dropped}, suitable for passing to \code{\link[=consort_plot]{consort_plot()}}.}
\item{\code{na_cells}}{A data frame with columns \code{step} and \code{group}
identifying step–group combinations that did not apply to a group's
source dataset. Pass this directly to the \code{na_cells} argument of
\code{\link[=consort_plot]{consort_plot()}}.}
}
}
\description{
Combines two or more tracker tibbles produced in separate environments or
from separate datasets into a single tracker ready for \code{\link[=consort_plot]{consort_plot()}}.
Groups that are missing a step (because that criterion did not apply to
their dataset) are carried forward at their last known count and flagged
automatically in the returned \code{na_cells} data frame.
}
\examples{
cgd <- prep_cgd_example()

# Two separate datasets processed independently
tracker_eu <- cgd |>
  dplyr::filter(region == "Europe") |>
  new_cohort("Randomised", id_col = "id", group_col = "region") |>
  include_if(age >= 5,     "Age >= 5 years") |>
  include_if(weight >= 15, "Weight >= 15 kg") |>
  get_tracker()

tracker_na <- cgd |>
  dplyr::filter(region == "North America") |>
  new_cohort("Randomised", id_col = "id", group_col = "region") |>
  include_if(age >= 5, "Age >= 5 years") |>
  get_tracker()  # weight step not run for this dataset

result <- merge_trackers(tracker_eu, tracker_na)
\dontrun{
consort_plot(result$tracker, na_cells = result$na_cells)
}
}
