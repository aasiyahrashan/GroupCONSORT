% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attrition.R
\name{update_data}
\alias{update_data}
\title{Replace cohort data without modifying the tracker}
\usage{
update_data(cohort, new_data)
}
\arguments{
\item{cohort}{A \code{cohort} object from \code{\link[=new_cohort]{new_cohort()}}.}

\item{new_data}{Replacement data frame. Must still contain the column named
by \code{cohort$id_col}.}
}
\value{
The updated \code{cohort} object with \verb{$data} replaced by \code{new_data}.
\verb{$tracker}, \verb{$id_col}, and \verb{$group_col} are unchanged.
}
\description{
Use between \code{\link[=include_if]{include_if()}} steps when mutations need to be applied to
\code{cohort$data} -- for example, data.table joins or column derivations --
without recording an attrition step.
}
\examples{
cgd <- prep_cgd_example()
cohort <- new_cohort(cgd, label = "Randomised", id_col = "id",
                     group_col = "region") |>
  include_if(age >= 5, "Age >= 5 years")

mutated <- dplyr::mutate(get_data(cohort), bmi = weight / (age ^ 2))
cohort <- update_data(cohort, mutated) |>
  include_if(bmi < 30, "BMI < 30")

}
