% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attrition.R
\name{include_if_grouped}
\alias{include_if_grouped}
\title{Apply a per-patient inclusion filter and record the attrition step}
\usage{
include_if_grouped(cohort, condition, label)
}
\arguments{
\item{cohort}{A \code{cohort} object from \code{\link[=new_cohort]{new_cohort()}}.}

\item{condition}{An unquoted filter expression evaluated per patient group.}

\item{label}{Display label for this step.}
}
\value{
The updated \code{cohort} object.
}
\description{
Like \code{\link[=include_if]{include_if()}}, but evaluates \code{condition} within groups defined by
\code{id_col}. Use when the condition involves window or aggregate functions
that should be scoped per patient, e.g.
\code{icu_admission_datetime == min(icu_admission_datetime)} to keep only each
patient's first ICU admission row.
}
\examples{
cgd <- prep_cgd_example()
cohort <- new_cohort(cgd, label = "Randomised", id_col = "id",
                     group_col = "region") |>
  include_if(age >= 5, "Age >= 5 years") |>
  include_if_grouped(
    follow_up_days == min(follow_up_days),
    "First observation per patient"
  )

}
