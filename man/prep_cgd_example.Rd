% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example.R
\name{prep_cgd_example}
\alias{prep_cgd_example}
\title{Prepare \code{survival::cgd} for a consortr demonstration}
\usage{
prep_cgd_example()
}
\value{
A tibble with 128 rows and columns \code{id}, \code{center}, \code{region}, \code{age},
\code{weight}, \code{sex}, \code{inherit}, \code{steroids}, \code{treat}, \code{status},
\code{follow_up_days}.
}
\description{
Reduces the CGD recurrent-events dataset to one row per patient and adds a
\code{region} column grouping the 14 trial centres into North America and Europe.
\code{survival} ships with every R installation so no extra packages are needed.
}
\examples{
cgd <- prep_cgd_example()
cohort <- new_cohort(cgd, label = "Randomised", id_col = "id", group_col = "region") |>
  include_if(age >= 5, "Age >= 5 years") |>
  include_if(weight >= 15, "Weight >= 15 kg") |>
  include_if(steroids == 0, "Not on corticosteroids") |>
  include_if(follow_up_days > 0, "Positive follow-up time")
consort_plot(cohort)

}
