# Extract the data frame from a cohort object

Extract the data frame from a cohort object

## Usage

``` r
get_data(cohort)
```

## Arguments

- cohort:

  A `cohort` object.

## Value

The filtered data frame at the current stage.
